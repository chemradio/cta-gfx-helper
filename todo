to docker 

- container 1: bot + db
- container 2: chrome screenshooter
- container 3: chrome video-html
- container 4: file server

ffmpeg -i ./input.mp4 -vf "scale=640:640:force_original_aspect_ratio=decrease,pad=ceil(iw/2)*2:ceil(ih/2)*2" -vcodec libx264 -crf 17 -pix_fmt yuv420p -bsf:v h264_metadata=video_full_range_flag=0:colour_primaries=9:transfer_characteristics=18:matrix_coefficients=9 ./output.mp4


self_dispatcher


migrate to sql?
quote checker
missing log files create first
rewrite logging
last x lines of std out to block_logger


/exit - для редактора - это команда удалит все изменения в цитате и вернется к самому первому шагу
main text handler - обработать ответы от редактора

purge db orders
figure out how to store allowed users




; cookies

parse url
    if scroll
        extract domain

get_domain - twitter, or hrw

open saved cookies

check if url in cookies

add saved cookies to driver

open url

dump cookies to storage


